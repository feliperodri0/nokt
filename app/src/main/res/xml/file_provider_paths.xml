<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Internal storage: app-specific files (attachments copied from external storage) -->
    <files-path
        name="attachments"
        path="attachments/" />
    <!-- External storage (needed for camera captures, if used in the future) -->
    <external-files-path
        name="external_attachments"
        path="attachments/" />
</paths>
